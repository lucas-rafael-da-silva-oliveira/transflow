# Example environment variables for TransFlow
# DO NOT commit real secrets to version control. Use this file only for documentation

# MongoDB (example for local Docker)
MONGO_URI=mongodb://mongo:27017
MONGO_DB=transflow

# Redis
REDIS_HOST=redis
REDIS_PORT=6379

# RabbitMQ (example of host + user; do NOT use guest credentials in production)
RABBIT_URL=amqp://user:password@rabbitmq:5672/

# Application name
APP_NAME=TransFlow Backend (example)
